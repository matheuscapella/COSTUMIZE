<%# <%= cl_image_tag @costume.photo.key, height: 300, width: 400, crop: :fill %>
<body style = "background-color: #94b8b8">
  <div class="container mt-5 text-center">
    <h1><%= @costume.title %></h1>
    <div class="col d-flex justify-content-center">
      <div class="card h-100">
       <%= cl_image_tag @costume.photo.key, height: 300, width: 400, crop: :fill%>
      </div>
    </div>
      <h2 class= mt-3>$<%= @costume.price %></h2>
      <p><%= @costume.description %></p>
    <div class="btn btn-primary">Reserve Costume
    </div>
      <br>
        <%= link_to "Back to Index", costumes_path  %>
  </div>
  <div>
    <% if @costume.user == current_user %>

    <% end %>

  </div>
</body>
