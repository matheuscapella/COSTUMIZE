<div class="container">
  <%= simple_form_for(@costume) do |f| %>
    <%= f.input :photo, as: :file %>
    <%= f.input :price %>
    <%= f.input :description %>
    <%= f.submit %>
  <% end %>
  <%= link_to "Back to Index", costumes_path  %>
</div>
